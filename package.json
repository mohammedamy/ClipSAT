{
  "name": "clipsat",
  "version": "1.0.0",
  "description": "ClipSAT — Math by Mr. Mohamed Abdallah",
  "scripts": {
    "build": "node build.js && npx @11ty/eleventy",
    "start": "npx @11ty/eleventy --serve",
    "extract": "node build.js"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
