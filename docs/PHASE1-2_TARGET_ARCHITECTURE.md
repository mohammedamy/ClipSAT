# ClipSAT — PHASE 1 & 2: Token-Efficiency Principles + Target Architecture Proposal

**Date:** 2026-08-18
**Status:** Proposal. Nothing in the repo has been changed. **Stop-and-approve gate per the plan — do not proceed to Phase 3 until you sign off.**
**Builds on:** [`docs/INVENTORY.md`](INVENTORY.md) (Phase 0 baseline).

---

## PHASE 1 — Token-efficiency principles, adapted to ClipSAT

| # | Principle | Concrete implementation here | Token saving | Cost |
|---|---|---|---|---|
| 1 | Single-entry orientation | Promote `CLAUDE_CODE_STARTER_PROMPT.md` (already 85 lines, already does 80% of this job) to `AGENTS.md` at repo root. Trim to index-only: what/where/how/conventions/commands, linking out to `docs/RECIPES.md` and `docs/CONTENT_MODEL.md` for detail instead of inlining it. | **High** — this file already drives the >90% token reduction measured in Phase 0 §8; making it canonical and auto-discovered removes the "did the session remember to read it" risk. | **Low** — mostly a rename + trim + link-out; content already exists. |
| 2 | Locate-by-path | One content file per chapter: `content/{track}/{chapter-slug}.json`. Track slug and chapter slug are both already stable IDs in `bank-data/*.json`'s `domain` field and the existing chapter markup — no new naming scheme invented, reusing what's there. | **High** — "add a lesson to qudrat ch.6" resolves to `content/qudrat/6.01-sequences-patterns.json` by reading the task description alone, zero grep. | **Medium** — requires the content migration itself (Phase 4), not a bolt-on. |
| 3 | Small files, one concern | Chapter-level JSON (not course-level) keeps individual files small by construction — the current largest single track template (`calculus.html`, 1,912 LOC) covers 19 chapters; split per-chapter that's ~100 LOC of *data* each (markup moves to a shared template, counted once). MCQ pools stay in `bank-data/*.json` (already separate, already fine — not subject to the 300-LOC cap since they're pure data, not source a human hand-edits line by line). | **High** for the 41K-line monolith specifically. | **Medium** — the migration work itself. |
| 4 | Content ⟂ presentation ⟂ logic | Extend `course_schema.json` (see Phase 2 §3) — it already encodes this separation correctly, it's just never been adopted. Presentation moves to a small set of Nunjucks partials (chapter, definition-box, theorem-box, worked-example, quiz-checkpoint); the 56 canvas explorers' *logic* stays exactly where it is (`engine.js`) per the roadmap's explicit "don't rebuild the explorer engine" — only their *host markup* (a `<canvas id>` + wrapper) becomes templated. | **High** | **Medium-High** — the core of Phase 4. |
| 5 | Single source of truth for design | `public/css/main.css`'s 87-token layer already exists and is already the *intended* single source — the gap is 215 hardcoded hex values coexisting in the same file. A mechanical find-and-replace-with-token pass (scriptable, verifiable by visual diff) closes this without inventing a new token system. | **Medium** (rebrand task only) | **Low** — mechanical, scriptable, one file. |
| 6 | Contracts over implementations | Add a JSDoc block (params, return, side effects) to the top of `build.js`'s remaining functions, `engine.js`'s public entry points (`CS_loadTrackBank`, `genChapterQuiz`, `genTest`, etc. — named in `CLAUDE_CODE_STARTER_PROMPT.md`), and `cloud-sync.js`. No TypeScript conversion — that would violate "no new dependency without justification" for a codebase this size with one maintainer. | **Medium** | **Low-Medium** — additive, no behavior change. |
| 7 | Aggressive de-duplication | Retire the footer/nav dual-edit (`index.html` vs. `build.js`'s `baseNjk` string) — see Phase 2 §6 WP3. This is the single highest-leverage de-dup: it's caused a real bug once already. | **Medium** (this specific pattern), but High risk-reduction. | **Low** — one structural change, well-scoped. |
| 8 | Templating over repetition | The 9×/3× duplicated boilerplate paragraphs (`AUDIT.md` DUP-002/003, still unresolved per Phase 0 §3) become single strings in the new chapter/quiz-checkpoint partials once content moves out of raw per-track HTML. | **Low-Medium** (small in raw bytes, but it's the textbook case the principle exists for) | **Low** — falls out of Phase 4 migration for free. |
| 9 | Context exclusion | Add `.aiignore` (or equivalent) excluding: `node_modules/`, `_site/`, `public/downloads/` (1,880 PDFs — never needs reading), `*.pdf`, `package-lock.json`, `.claude/worktrees/`, and the 5 untracked personal folders confirmed out-of-repo in Phase 0. | **High** for any full-tree operation (Explore-style search, `git grep` across the working directory). | **Trivial** — one file, no code change. |
| 10 | Machine-readable structure | `docs/MAP.json`: `{ "path": { "purpose": "...", "owner": "content\|engine\|build\|docs" } }`, generated once by hand then kept current as part of the Phase 4 migration checklist (one line added per new chapter file — mechanical, not a research task). | **Medium** | **Low** — small, mechanical to maintain once seeded. |
| 11 | Task recipes | `docs/RECIPES.md` — directly operationalizes Phase 0 §8's benchmark table into "open these exact files, in this order" steps per task. This is the doc that turns the "grep-first agent" behavior (currently tacit, dependent on `CLAUDE_CODE_STARTER_PROMPT.md` being read carefully) into an explicit, checkable recipe. | **High** — closes the exact gap Phase 0 §8/§9 identified: today's savings are discipline-dependent, not structural. | **Low** — writing, not code. |
| 12 | Deterministic quality gates | Currently **zero** lint/test/validate tooling exists (`package.json` has only `@11ty/eleventy`, `csso`, `uglify-js`). Add: (a) `ajv`-based schema validation for every `content/**/*.json` against `course_schema.json`, run in CI before build; (b) a small script that auto-derives the 3 cache-bust version strings from a git short-hash instead of hand-editing `main.css`/`engine.js`/`sw.js` on every PR (closes Phase 0's risk-register item #3); (c) keep `npx @11ty/eleventy` build success as the existing green-build gate. **New dependency justification:** `ajv` — replaces "hope the JSON is shaped right," costs ~130 KB dev-dependency, no runtime cost (build-time only). | **Medium** (verification cost, not read cost) but removes a standing manual-error risk. | **Low-Medium** — `ajv` is a single well-known dependency; the version-bump script is ~20 lines. |

---

## PHASE 2 — Target architecture proposal

### 2.1 Stack recommendation

**Two options, honest trade-off:**

| | **Option A — Keep Eleventy, decompose the monolith (Recommended)** | **Option B — Migrate to Astro (content collections)** |
|---|---|---|
| What changes | `index.html`'s content moves into `content/{track}/*.json`; `build.js` shrinks from a 956-line extraction engine to a small cache-bust helper (or is retired entirely); Nunjucks templates read the content via Eleventy's native data cascade instead of generated includes. Hosting, URLs, and the explorer engine (`engine.js`) are untouched. | Same content decomposition, but rendered via Astro's content collections (built-in schema validation, no hand-rolled `build.js` extraction) and `.astro` components instead of Nunjucks. |
| Maintainability (non-engineer solo teacher) | High — same mental model as today (edit a file, run one build command), just a smaller and more numerous set of files. | Medium — Astro's component model, frontmatter, and TS-flavored props are a bigger conceptual jump for a non-full-time-engineer maintainer than Nunjucks partials. |
| Build simplicity | High — `npm run build` stays a 2-step command; CI config barely changes. | Medium — new build tool, new CI step, new failure modes to learn during the switch. |
| Bilingual/RTL support | Neutral — this is a content-model concern, not a framework concern; both frameworks render whatever `dir`/`lang` the template sets. | Neutral, same reasoning. |
| Static-first / hosting | Fully compatible with the existing GitHub Pages + Actions pipeline, zero hosting change. | Also static-output-capable, but is a different enough tool that "no migration off static-first hosting" (roadmap constraint) is easy to accidentally violate later, since Astro's default trajectory invites SSR/edge features ClipSAT doesn't need. |
| AI-agent navigability | High — fewer moving parts than today, same template language agents already have PR history editing successfully (4 recent Arabic PRs all touched Nunjucks-adjacent files without issue). | Unknown/untested for this codebase — would reset the "agent already knows this pattern" advantage documented in Phase 0 §8. |
| Switching cost | Low — additive to what exists. | High — rewrite of 21 templates, `build.js`, CI, and re-validation of every explorer's mounting logic, for a benefit (built-in content-collection validation) that Option A gets more cheaply via `ajv` (Phase 1 principle #12). |
| Conflicts with existing constraints | None. | Directly in tension with the product roadmap's explicit **"don't migrate off static-first hosting, don't rebuild the explorer engine"** — a full framework swap is exactly the kind of change that guidance exists to prevent, even though Astro itself can be used in a static-only mode. |

**Recommendation: Option A.** The actual problem Phase 0 identified — content trapped inside a 41K-line HTML file — is a data-modeling problem, not a framework problem. Eleventy already solves the "generate 21 static pages from templates" job correctly; switching frameworks would pay a large, real cost to re-solve a problem that isn't the bottleneck, while risking the exact things the maintainer has already told future sessions not to touch.

### 2.2 Target directory tree

```
clipsat/
├── AGENTS.md                        # ≤200 lines, single entry point (Phase 1 #1)
├── build.js                         # shrinks: cache-bust helper only, no more content extraction
├── content/                         # NEW — the decomposed monolith
│   ├── {track}/
│   │   ├── _meta.json                # course-level meta (title, level, color, prerequisites…)
│   │   └── {chapter-slug}.json       # one chapter, ≤300 LOC, schema-validated
│   └── pages/                        # non-course static pages' copy, if/when they outgrow inline njk
├── course_schema.json               # REVIVED — extended with bilingual + video + explorer refs (§2.3)
├── docs/
│   ├── ARCHITECTURE.md
│   ├── MAP.json
│   ├── CONTENT_MODEL.md
│   ├── RECIPES.md
│   ├── DESIGN_TOKENS.md
│   ├── DEPLOY.md
│   ├── DECISIONS/
│   └── INVENTORY.md                 # this Phase 0 baseline, retained as historical record
├── public/
│   ├── css/main.css                 # unchanged location; hex→token sweep applied (Phase 1 #5)
│   ├── js/
│   │   ├── engine.js                # unchanged — explorer/quiz logic, NOT rebuilt
│   │   ├── cloud-sync.js, cloud-config.js, teacher-view.js   # unchanged, already fine
│   └── downloads/                   # unchanged — 1,880 PDFs, excluded from AI context (Phase 1 #9)
├── src/
│   ├── _includes/
│   │   ├── base.njk                 # now HAND-AUTHORED and authoritative — no more baseNjk string in build.js
│   │   ├── partials/                # NEW: chapter.njk, definition-box.njk, theorem-box.njk,
│   │   │                            #      worked-example.njk, quiz-checkpoint.njk, explorer.njk,
│   │   │                            #      video-embed.njk, downloads-block.njk, test-generator.njk
│   │   └── legal/                   # unchanged
│   ├── {track}/index.njk × 21       # unchanged shape (8 LOC), now actually reads content/{track}/
│   └── (contact/, privacy/, terms/, cookies/, 404.html, index.njk — unchanged)
├── bank-data/*.json                 # UNCHANGED — this pattern already works, not touched
├── app.py                           # UNCHANGED, but documented in docs/DEPLOY.md as a SEPARATE deployable
├── .aiignore                        # NEW (Phase 1 #9)
├── scripts/
│   └── migrate-chapter.js           # NEW — the Phase 4 migration script (index.html → content/*.json)
└── .github/workflows/deploy.yml     # unchanged shape, adds an ajv-validate step before build
```

**What's explicitly NOT changing:** URL structure, hosting, `bank-data/*.json`, `engine.js`'s explorer/canvas logic, the cloud-sync/Supabase layer, `app.py`'s role, the deploy pipeline's shape.

### 2.3 Content model

Extends the existing (currently-unused) `course_schema.json` rather than inventing a new one — it already gets 90% of this right. Changes needed, field by field:

| Field | Current schema | Change | Why |
|---|---|---|---|
| Every user-facing text field (`term`, `body`, `statement`, `problem`, step `text`, `title`, `description`, quiz `text`/choice `text`) | Plain `string` | Becomes `{ "en": string, "ar": string \| null }`. `ar: null` is valid and means "not yet translated" — renders English with no Arabic toggle option for that specific string, matching today's real partial-rollout state (qudrat/tahsili chapters translated incrementally, most tracks still English-only). | **Non-negotiable constraint #4 (bilingual integrity)** — the current schema has zero bilingual fields, confirmed by grep in Phase 0. This must be designed, not retrofitted later. Chosen over the current runtime i18n-dictionary-key pattern (`data-i18n-html="qud.about.intro"` + a separate giant JS dictionary object) because inline `{en, ar}` pairs keep a chapter's translation status visible and diffable in the same file, instead of split across content and a 900-line dictionary blob that can silently drift out of sync with its keys. |
| *(new)* `meta.dir` per language | — | Not needed as a field — `dir="rtl"` is derived from `lang === "ar"` in the shared template, not stored per-chapter. | Avoids a value that could contradict its own `lang`. |
| `content.videos` | Does not exist | New array: `{ "id", "provider": "youtube", "videoId", "title": {en, ar}, "topicTag" }`. | Benchmark task 6 (embed a video) currently has no dedicated field — it's presumably hand-inserted as an `<iframe>` in `index.html` today (only 2 YouTube references found repo-wide, confirming this is a thin/early feature, exactly where getting the schema right *now* is cheapest). |
| `content.explorers` | Does not exist | New array: `{ "id", "canvasId", "type", "description": {en, ar} }` — a **reference only**. The actual canvas drawing/interaction code stays in `public/js/engine.js`, keyed by `canvasId`, per the "don't rebuild the explorer engine" constraint. This field exists so a template knows *where* to mount an explorer and *what to call it*, nothing more. | Keeps content and engine logic separated without touching the 56 existing canvas implementations. |
| `chapters[].quiz` | `chapterQuiz` (in-page checkpoint quiz, a handful of questions) | Unchanged — stays small, inline, bilingual per the string-field rule above. | Already correctly separated from the large `bank-data/*.json` pools; no change needed to that separation. |
| Large MCQ pools | N/A — lives in `bank-data/{track}.json`, own ad hoc schema (`pool`/`sections`/`letters`) | **Unchanged.** Not folded into `course_schema.json`. | This is the one part of the current architecture that already fully matches the target pattern (pure data, fetched at runtime, zero code change to add a question) — Phase 0 §5 flagged this explicitly as proof the pattern works. No reason to touch a working thing. |
| `pages` (contact/privacy/terms/cookies) | N/A | **Stay as small hand-authored `.njk` files**, not migrated to JSON. | They're already ≤44 LOC each (Phase 0 §1) and rarely change — moving them to data would add indirection with no token or maintainability benefit. |

### 2.4 Component inventory (Nunjucks partials, `src/_includes/partials/`)

| Component | Props | Notes |
|---|---|---|
| `chapter.njk` | `chapter` (one `content/{track}/{slug}.json` object), `lang` | Top-level chapter renderer; composes the boxes below. |
| `definition-box.njk` | `definitions[]`, `lang` | |
| `theorem-box.njk` | `theorems[]`, `lang` | |
| `worked-example.njk` | `workedExamples[]`, `lang` | Collapsible steps, matches current UI pattern. |
| `quiz-checkpoint.njk` | `quiz.questions[]`, `lang` | In-chapter checkpoint only — NOT the full bank-data-driven generator. |
| `explorer.njk` | `explorer` (one entry from `content.explorers[]`) | Renders `<canvas id="{{explorer.canvasId}}">` + a lazy-load hook into `engine.js`; does not contain explorer logic itself. |
| `video-embed.njk` | `video` (one entry from `content.videos[]`), `lang` | |
| `downloads-block.njk` | `trackId` | Reads the track's `public/downloads/{trackId}/` listing; replaces the 19 hand-copied "Downloads" sections (Phase 0 §3). |
| `test-generator.njk`, `practice-set.njk` | `trackId` | Replaces the 16/14 hand-copied sections; collapses DUP-002/003 (Phase 0 §3) into one template each. |
| `base.njk` | `title`, `description`, `trackId`, page body | Now hand-authored directly — no more `baseNjk` string living inside `build.js`. This is what retires the footer/nav duplication (§2.6 WP3). |

### 2.5 Routing and URL map

**No URL changes.** Current routes (`https://mohammedamy.github.io/ClipSAT/{track}/`, `pathPrefix: "/ClipSAT/"` in `.eleventy.js`) are preserved exactly — this is a content-model refactor underneath the existing routing, not a routing change. **Redirect table: empty, none needed.** This satisfies non-negotiable constraint #2 (no URL breakage) by construction rather than by adding redirect rules.

### 2.6 Before → after mapping (representative — full 21-track table follows the same pattern)

| Current file | Destination | Rationale |
|---|---|---|
| `index.html` (content for track T, chapter C) | `content/T/C.json` | Core Phase 4 migration, one track at a time. |
| `index.html` (footer/nav markup) | *(deleted — content moves into `src/_includes/base.njk`, which becomes the only copy)* | Retires the confirmed dual-edit bug source (Phase 0 §2). |
| `index.html` (as a whole, once all 21 tracks migrated) | **Delete**, after Phase 5 parity sign-off — replaced by `content/**` + `src/_includes/**`. Irreversible; flagged for explicit approval before it happens (WP7, not before). | The "golden rule: index.html is the only source of truth" retires once content has a real home. |
| `build.js` (extraction logic: CSS/JS/HTML pulling out of `index.html`) | **Delete** once WP7 lands — no longer needed, Eleventy's native data cascade replaces it. | Extraction only exists because content lives somewhere extraction is needed *from*. |
| `build.js` (cache-bust logic) | `scripts/bump-version.js` (new, small) | Survives as its own concern, now automated (Phase 1 #12) instead of hand-edited. |
| `src/_includes/tracks/*.html` (21 files, currently generated) | **Delete** — replaced by `partials/chapter.njk` + `content/{track}/*.json` rendered through it. | These exist only because content was still trapped in `index.html`; once it isn't, the generated-include layer is redundant. |
| `course_schema.json` | **Kept, extended** (§2.3) | Already correct in spirit, just never adopted. |
| `bank-data/*.json` | **Unchanged.** | Already the target pattern. |
| `public/js/engine.js`, `cloud-sync.js`, `cloud-config.js`, `teacher-view.js` | **Unchanged.** | Not in scope — logic layer, not content layer. |
| `course-loader.js`, `router.js`, `storage.js`, `desmos-widget.js` | **Delete**, after explicit confirmation (Phase 0 §4 flagged these as high-confidence dead code, but deletion is irreversible and needs your go-ahead first). | Not referenced by anything except a stale service-worker precache list. |
| `courses/` (currently just `.gitkeep`) | Either **repurposed** as the new `content/` root (rename), or left alone and `content/` created fresh — **your call, ask before Phase 3.** | Ambiguous pre-existing intent (Phase 0 §4); don't want to guess. |
| `app.py` | **Unchanged**, documented properly in new `docs/DEPLOY.md` as a second, separately-hosted deployable. | Phase 0 risk #5 — needs to stop being invisible to the build docs, not to be touched. |

### 2.7 Migration sequence — ordered, independently shippable, each reversible

| WP | What | Ships as | Risk | Rollback |
|---|---|---|---|---|
| **WP1** | `AGENTS.md`, `docs/RECIPES.md`, `docs/MAP.json`, `docs/DESIGN_TOKENS.md`, `.aiignore` (Phase 1 #1, 9, 10, 11) | 1 PR, purely additive | None — no existing file's behavior changes | `git revert` |
| **WP2** | Hex→token sweep in `public/css/main.css` (Phase 1 #5) | 1 PR | Low — visual regression risk, catch via screenshot diff on a few tracks before merge | `git revert` one file |
| **WP3** | ~~Retire~~ **Scoped down on inspection — see `docs/DECISIONS/0001-shell-sync-check-not-removal.md`.** Could not confirm whether `index.html`'s header/footer copy is still used for direct-preview convenience, so did not remove it. Shipped instead: fixed a second real drift found while auditing (`cloudSignInBtn`/`teacherViewBtn` missing from `index.html`'s copy), and added `scripts/check-shell-sync.js` (wired into `npm run build`/CI) so future drift fails the build instead of shipping silently. The original framing (retire one copy entirely) is still open, pending your call. | 1 PR | Low — no behavior change to the deployed site; the new check only adds a build-time gate | `git revert` |
| **WP4 — shipped** | Deleted confirmed dead code (`course-loader.js`, `router.js`, `storage.js`, `desmos-widget.js`) and their `sw.js` precache entries, after explicit confirmation. Re-verified dead-code status immediately before deleting (fresh `git grep`, zero real references found). Bonus finding while doing so: `.eleventy.js` never copied these into `_site/`, so `sw.js`'s atomic `cache.addAll()` shell pre-cache had been silently failing on every install because of the 404s — fixed in the same PR, `SW_VERSION` bumped. | 1 PR | Low technical risk, confirmed via fresh re-verification; the "maybe future feature" possibility from Phase 0 §4 was accepted as a risk by explicit sign-off | Files recoverable from git history even after deletion |
| **WP5 — shipped** *(the `explorers[]` array described here was superseded by the `explorer` blocks[] kind — see `docs/DECISIONS/0003` and WP6 status below; kept as historical record)* | Extended `course_schema.json` with the bilingual `{en,ar}` fields, `videos[]`, and `explorers[]` designed in §2.3 (documented in the new `docs/CONTENT_MODEL.md`); added `ajv` (justified: replaces "hope the JSON is shaped right," ~130 KB dev-dependency, build-time only) and `scripts/validate-content.js`, wired into `npm run build`/CI. Validates the schema's own `examples` too, so schema and docs can't silently drift apart. `content/` doesn't exist yet — the validator reports "nothing to validate" cleanly rather than failing. | 1 PR | None — validates nothing yet, just wires the gate | `git revert` |
| **WP6 (pilot) — qudrat's content is 100% migrated; schema evolved 3× on real evidence** | The schema changed shape twice while converting real qudrat content, each time on concrete evidence rather than up-front guessing: (1) `docs/DECISIONS/0002` — the original fixed definitions/theorems/keyFormulas/workedExamples split didn't match real markup at all; replaced with an ordered `blocks[]` model (`callout`/`table`/`cards`/`text` kinds, plus `example-card` added when ch.6/ch.8 showed a real 4th callout shape). (2) `docs/DECISIONS/0003` — interactive explorers needed their own `explorer` blocks[] kind, an exact-shell (not generic) reference into `engine.js`'s existing per-explorer JS, verified against all 3 real qudrat explorer instances. **All of qudrat is now migrated and browser-verified:** 9/9 chapters (`qud-about/arithmetic/algebra/geometry/patterns/data/word/ratio/compare`), the Downloads section (`downloads-block.njk`, also fixed a real unclosed-`<div>` bug present across all 21 tracks), and the 50-problem practice set (`practice-set.njk`, new `practiceSet` schema shape, verified the page's *existing, unmodified* `toggleSol()` JS correctly drives the new markup). Bilingual fidelity: chapters/sections with real Arabic in the source got real Arabic (`qud-about/arithmetic/algebra/geometry/ratio`, spot-checked against source); chapters genuinely untranslated in the source stayed `ar: null` (`qud-patterns/data/word/compare`, `qud-downloads`/`qud-practice` copy) — nothing invented either direction. | 6 PRs so far | Medium — two schema corrections, each caught before it cost real migrated content | `index.html`'s qudrat section + `tracks/qudrat.html` completely untouched; `content/qudrat/` and the new partials are pure additions the live site doesn't read yet |
| **WP7 — shipped: migrated content now drives the site's real language toggle** | Wired `content/*.json`'s bilingual pairs into the *same* `window.i18n` toggle every legacy page already uses — no second mechanism. Design: `docs/DECISIONS/0004-bilingual-dom-toggle-not-dictionary-injection.md`. Both languages render into the DOM (a shared `_bilingual.njk` macro emits `.i18n-en`/`.i18n-ar` pairs); a small, purely-additive extension to `index.html`'s `_applyToDOM()` (the function `setLocale()` already calls) toggles which one is visible — verified as a guaranteed no-op on every pre-existing page (new selectors match zero elements there). Rewrote all 5 block partials + `chapter.njk` to use it; quiz-widget labels reuse the *existing* dictionary keys instead (they already exist, no reason to duplicate). **Verified end-to-end, not just unit-checked:** regression-tested the live `/qudrat/` page's existing toggle before/after (identical); clicked the *actual* header 🌐 button (not a programmatic call) on `/wp6-verify/` and confirmed full-page RTL mirroring, correct Arabic across all 5 translated chapters, correct English fallback in the 4 untranslated ones, correct `aria-label` switching, MathJax re-typesetting on toggle. **This verification pass itself caught a real bug**: two explorer `ariaLabel` fields in `qud-ratio.json` were hardcoded English-only despite real Arabic existing in the source — a value that doesn't change under the real toggle is an obvious, loud signal; fixed before shipping. | 1 PR | Medium-High — the first change in this whole migration to touch shared, currently-live JS (not just add new unused files); mitigated with an explicit before/after regression check on a real page, not just "should be fine" | `index.html`'s `_applyToDOM()` (additive only — 3 pre-existing scans untouched); `qud-ratio`/`qud-compare`/legacy pages all re-verified working after |
| **WP8 — shipped: `/qudrat/` cut over to render from `content/`** | The actual production cutover — the first change in this migration that changes what a real page serves, not just adds new files. Full account, including 3 previously-missed pieces found only by reading the legacy file's full structural fingerprint (a page `subject-head`, the nav `rail` wired to the site's existing `goChapter()`, and an entirely separate `qud-testgen` section) and 2 real bugs caught before shipping (a missing `<main id="view-qudrat">` wrapper — verification caught it via a null `getElementById`, not luck; `downloads-block.njk`/`practice-set.njk` deriving the wrong section id from `trackId` instead of using the real source id, which would have silently broken URL-hash deep links): `docs/DECISIONS/0005-qudrat-cutover.md`. New: `chapterOrder` in `_meta.json` (explicit display order — directory listing order isn't reliable), `migratedContent.js` (generic Eleventy data loader, any future migrated track just needs a `_meta.json` to show up), `build.js`'s `MIGRATED_TRACKS` set (stops `build.js` from clobbering the hand-maintained `index.njk` on every run), `rail.njk`/`subject-head.njk`/`test-generator.njk` partials. **`index.html`'s `qud-*` section and `tracks/qudrat.html` are deliberately kept, unused — the rollback path.** Verified: real rail-link clicks switch chapters correctly (URL hash updates too); `qud-ratio`'s canvas explorer actually draws and its slider live-updates correctly (dragged `qudRatioA` 2→7, confirmed the calculated share went 48→84); the Test Generator's "Generate test" button pulled 10 real questions from `bank-data/qudrat.json`; language toggle switches the whole assembled page together; console errors compared against the untouched `/calculus/` page to confirm zero new errors (same pre-existing dev-server noise on both). **Qudrat is now a complete, live, working pilot of the whole target architecture** — the next track to migrate should follow WP6→WP8 in order and expect to find its own version of WP8's 3 missing pieces by reading its full legacy markup, not by assuming qudrat's structure is universal. | 1 PR | High — the first change to affect real page output; mitigated with the most extensive verification pass in this migration (interactive clicks, slider drag, live bank-data test generation, full-repo diff review, console-error comparison against an untouched page) | `src/qudrat/index.njk` (rewritten), `build.js` (additive — `MIGRATED_TRACKS` skip only), `index.html`'s `qud-*` section and `tracks/qudrat.html` untouched and kept as rollback |
| **WP9 — in progress: roll out to the remaining tracks** *(renumbered from the original plan's "WP7" — collided with the actually-executed WP7/WP8 above; kept the original row's content, fixed only the label)* | One PR per track (or small batches), each independently shippable, following WP6→WP8's sequence exactly (structural fingerprint the legacy file fully first, watch for a page subject-head/rail/test-generator, watch for the `<main id="view-{track}">` wrapper, watch for real vs. `{{trackId}}`-derived section ids). **`tahsili` done** (2026-08-18) — same pattern held with zero new design surprises: 10 chapters (`tah-about` + 9 numbered, vs. qudrat's 9), 2 explorers (`tah-functions`, `tah-calc1`), 50-problem practice set, test generator, full cutover. One new, real, pre-existing content finding: the subject-head stat text says "Chapters · 8" but there are 9 numbered chapters — a genuine inaccuracy in the *original* site content, preserved exactly as-is rather than silently corrected (content fidelity — flagging a source error isn't the same as fixing it without being asked). Verified with the same rigor as WP8: real rail-link click switched chapters and the graph-transformation explorer's vertex readout live-updated correctly (dragged `tahH` 0→3, confirmed `(3, 0)`); language toggle correct (rail label translated, chapter's own untranslated title correctly stayed English — the two aren't always in sync in source, checked both independently, not assumed identical); console errors compared against `/calculus/`, zero new ones. **`act2` done** (2026-08-18, WP12) — 9 chapters, 3 explorers (`act2RatCanvas`/`act2LawCanvas`/`act2IncCanvas`), 50-problem practice set, test generator, full cutover; `docs/DECISIONS/0006` — a new `practiceProblem` block kind and `chapterTag`'s `category` variant, both genuinely new real shapes, not forced into qudrat/tahsili's existing ones. Migrating act2 also surfaced **three real, live, pre-existing bugs** in shared partials that had silently baked qudrat's own copy into `test-generator.njk`, `downloads-block.njk`, and `practice-set.njk` for every track — wrong text was already showing on the shipped tahsili page (PRs #99, #100, and the practice-set fix bundled into the act2 PR); fixed by moving that copy into real per-track schema fields. Explorer verified interactive: dragged act2's Law-of-Cosines angle slider to 120°, readout correctly recalculated to 14.80 (math checked by hand). **`est2` done** (2026-08-18) — 8 chapters (no separate "about-adjacent" numbers chapter this time, ch.1 is algebra directly), 3 explorers (`est2InvCanvas`/`est2UcCanvas`/`est2CircCanvas`), reused the `category` chapterTag variant and `practiceProblem` block kind act2 introduced (both held with zero changes needed — first real reuse confirms they generalize, not just fit act2). Verified interactive: set the unit-circle explorer's angle to 1.57 rad (≈π/2), readout correctly showed cos≈0.001, sin≈1.000, tan≈1255.77 (blowing up near the real asymptote, math checked by hand). **`est` done** (2026-08-18) — 8 chapters (same domain-weightings-table `about` pattern), 3 explorers (`estParabCanvas`/`estPythCanvas`/`estTrigCanvas`). Notably less uniform per-chapter block ordering than qud/tah/act2/est2 — some chapters open with a `callout thm` "Reference" box and never use `callout def`, others skip the early reference box and use `callout def` later — read and preserved the real order per chapter rather than assuming a template. Verified interactive: dragged the vertex-form parabola explorer to a=-1.5, h=1, k=-2, readout correctly reported vertex (1,-2) and "no real roots" (checked by hand: \(-k/a = -1.33 < 0\)). **`appc` done** (2026-08-18) — 5 chapters (overview + 4 units), 3 explorers. First track with genuinely different practice-set/test-generator markup and vocabulary (Easy/Medium/Hard vs the other 5 tracks' Basic/Intermediate/Advanced) — added a `compact` variant to both (`docs/DECISIONS/0007`), plus a new `workedExampleCard` block kind for its standalone problem+solution fcards, plus a functional `eyebrowClass` field for a real `.eyebrow.amber` CSS rule the earlier `eyebrow` field couldn't express. All additions optional/defaulted — zero changes needed to the five already-migrated tracks (re-verified qudrat unaffected). **`apstats` done** (2026-08-19) — 5 chapters (overview + 4 units), 3 explorers. Confirmed the `compact` variant is shared across College Board tracks (second real user, zero further schema changes) — but its chapterTag is the `category` variant (📌 emoji + weight badge + topic list), not appc's `pill`, despite sharing the exact same `ap-tag`/`ap-wt` class names; verified the real markup shape per track rather than assuming from a shared class name. Verified interactive: real slider drag on the mean-vs-median explorer to outlier=65, readout correctly computed mean=25.83, median=19.5 (checked by hand). **`apbc` done** (2026-08-19) — 9 chapters (overview + beyond + 6 BC-numbered + a dedicated worked-FRQ chapter), 3 explorers, 50 practice items. `docs/DECISIONS/0008`: found a `practiceSet.items[].levelLabel` need (badge shows "MCQ"/"FRQ" independent of the Basic/Intermediate/Advanced color class) and that even `rail.njk`'s "site-wide" practice-link text/icon has a real per-track exception ("Exam practice"/⊘). Also confirmed `compact` is genuinely optional within the AP family — apbc's testGenerator is fully `standard`. All additions optional/defaulted, qudrat re-verified unaffected. Verified interactive: Taylor-polynomial explorer at Terms=5 correctly computed polynomial degree 9 (5 odd-power terms of sin's Maclaurin series, checked by hand). **`act` done** (2026-08-19) — 9 chapters (about + 8 numbered). Found and fixed a live bug while preparing this track's downloads section: downloads-block.njk hardcoded the DOCX card's label/heading, wrong on appc's and apstats' already-shipped pages (shipped separately as PR #107, ahead of this migration) — act itself needed the same new fields, since its DOCX card describes the practice set specifically ("Practice worksheet"/"DOCX · editable"), not the whole chapter content. Everything else in this track is fully `standard` (chapterTag `category` variant matching act2/apstats, practiceProblem blocks, standard practice-set/testGenerator). Verified interactive: vertex-form explorer set to h=2, k=-3, readout correctly reported vertex (2, -3) and axis x=2. **`precalc` done** (2026-08-19) — 8 chapters, no "about" chapter (starts directly at ch.1), no chapterTag anywhere, no in-chapter practice-problem block of any kind (every chapter is callout/text/explorer/workedExampleCard only) — the densest track yet, 31 workedExampleCards across 8 chapters. `compact` practiceSet/testGenerator confirmed NOT AP-exclusive (precalc isn't a College Board track). 7-item practice set (compact) extracted via the established regex-script method. Verified interactive: unit-circle explorer set to θ=120°, readout correctly computed cos=-0.500, sin=0.866, tan=-1.732, Quadrant II. **Halfway point crossed (10/21).** Before starting the 11th track, found+fixed a real, live bug spanning ALL prior tracks (PR #110): `explorer.njk`'s `readouts` loop hardcoded every readout `<span class="v">` to a literal em-dash regardless of source — correct for qudrat/tahsili (real source genuinely starts at "—") but silently wrong on act2/appc/apstats/est/est2/precalc, which all have real precomputed defaults (e.g. apstats' `apsMean` really starts at "31.67"). Added `readouts[].value`, backfilled all 29 existing readout entries from real legacy HTML (scripted extraction), verified in-browser. `docs/DECISIONS/0009`. **`ibsl` done** (2026-08-19, WP23) — 7 chapters (overview + 5 numbered topics + an "AI SL — Specific Topics" chapter), 3 explorers, 12-item practice set. Found a SECOND real, live bug in the legacy source itself while reading it fully: each of the 6 numbered/AI topic `<section>` tags closes right after its Practice Problem + quiz widget, but 2–3 more worked examples plus 1 more practice problem for that SAME topic sit dangling outside any `.chapter` wrapper — since `.chapter{display:none}` by default, this content was rendering unconditionally regardless of active chapter tab. Restored into the chapter it topically belongs to (verified by subject match) rather than reproduced. Also: a third worked-example markup shape (`example-grid` callout style — `.example > .ex-body`, distinct from both `workedExampleCard` and the plain `example` style), a `chapterTag` pill whose 3 badges each carry a DIFFERENT class instead of one shared `badgeClassName`, and `testGenerator`/`downloadsInfo` `ib` variants (two "Generate exam" buttons sharing one sections/minutesArg; a resource-cards row replacing PDF/DOCX cards entirely, with the past-papers widget nested inside one specific card). `docs/DECISIONS/0010`. All additions optional/defaulted — re-verified qudrat and appc unaffected. Verified interactive: transformations explorer's `a` slider set to 15 (=1.5), readout equation correctly updated to "y = 1.5(x-0)² + 0". **`ibhl` done** (2026-08-19, WP24) — 8 chapters (overview + 5 numbered + AA HL and AI HL "Additional Topics" chapters, one more than ibsl's single AI chapter), 3 explorers, 12-item practice set. Same IB family as ibsl — reused ibsl's orphaned-content bug/fix, `example-grid` style, per-badge chapterTag classes, and `ib` testGenerator/downloadsInfo variants with ZERO further schema changes, confirming they generalize. Two genuinely new real deviations: (1) a lazy-loaded WebGL/Three.js 3D explorer shape (`explorerBlock.canvas3d` — a `🧊 View ... in 3D` toggle button instead of a plain canvas; a legacy-file check found this same shape 4 more times in not-yet-migrated `calculus`, so it's a real reusable site feature, not a one-off); (2) a "Curriculum Mapping" `<select>` widget in the rail, confirmed exclusive to ibhl among all 21 tracks (added as `rail.njk`'s optional `railExtra` raw-HTML passthrough rather than new schema fields for a one-instance feature). `docs/DECISIONS/0011`. All additions optional — re-verified qudrat unaffected. Verified interactive: real click on the 3D toggle button correctly mounted an actual `<canvas>` (Three.js renderer) inside the wrap and toggled aria-expanded/hidden. **`geo` done** (2026-08-19, WP25) — 11 chapters, 3 explorers, 50-item practice set. Simplest shape since precalc: no chapterTag, no in-chapter practice-problem block, plain `example` callout style throughout. Three new real deviations: `testGenerator.hasFullExam:false` (geo's real source has ONLY the mini practice-test controls, no "Full official exam paper" section at all — the first track found without one); `explorerBlock.readouts[].parts` (geo-right's Pythagoras explorer has one readout row, "a² + b²", whose `<span class="v">` holds TWO separately-updating ids joined by literal " + " text — confirmed via search this occurs nowhere else across all 21 tracks); `cardsBlock.items[].wide` (one card in a formula-cards row gets the wide class, siblings don't). Also made `controls[].valueDisplayId` optional (2 of geo's 3 sliders show no live value next to the label at all). `docs/DECISIONS/0012`. All additions optional — re-verified qudrat unaffected (full-exam section still renders). Verified interactive: dragged the Pythagoras explorer's Leg-a slider to 2 — compound readout correctly showed "4 + 16", independent c² row recalculated to 20. 8 tracks remain. | 36 PRs total, 13 shipped | Low per-PR once WP6-WP8 validated the pattern; still worth a full structural read per track, not a copy-paste — and worth re-auditing shared partials against each new track's real markup, since "shared" copy has twice now turned out to be one track's copy in disguise | Same as WP8, per track |
| **WP10** | Delete `index.html`, retire `build.js`'s extraction logic, delete `src/_includes/tracks/*.html` — **only after all 21 tracks migrated and Phase 5 sign-off** | 1 PR | High if premature, near-zero if done after full parity verification | Full history in git; this is the one step to be most conservative about timing |
| **WP9** | Phase 6 (Lighthouse/a11y/SEO pass — MathJax dedup, structured data expansion) | Several small PRs | Low, mostly independent of the content migration | Per-PR |
| **WP10** | Phase 7 docs finalization + Phase 8 token re-measurement against the Phase 0 §8 baseline | 1-2 PRs | None | Per-PR |

---

## Approval gate — RESOLVED 2026-08-18

All five decisions confirmed by the maintainer:

1. **Stack: Option A confirmed** — keep Eleventy, decompose the monolith. No Astro.
2. **Bilingual field design confirmed** — `{ en, ar: string|null }` inline pairs, replacing the runtime i18n-dictionary-key pattern for migrated tracks.
3. **`courses/` directory: leave alone** — `content/` is created fresh as its own top-level directory, not a repurposing of `courses/`.
4. **WP4 and WP8 (the two irreversible-in-spirit steps) are in the plan** — each still gated by its own explicit confirmation immediately before it executes, per the plan's "flag irreversibility" working rule. This sign-off authorizes planning and sequencing them, not executing them yet.
5. **Pilot track: `qudrat` confirmed.**

**WP1 in progress** — see PR for `AGENTS.md`, `docs/RECIPES.md`, `docs/MAP.json`, `docs/DESIGN_TOKENS.md`, `.aiignore`.
