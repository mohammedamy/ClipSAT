{
  "_purpose": "Machine-readable path -> purpose index. Not exhaustive for content assets (PDFs, per-track JSON) - see docs/INVENTORY.md for aggregate counts. Update this file when a path's role changes, not on every content edit.",
  "_lastUpdated": "2026-08-18",

  "AGENTS.md": { "purpose": "Single-entry orientation doc for AI sessions and new contributors", "owner": "docs" },
  "CLAUDE_CODE_STARTER_PROMPT.md": { "purpose": "Predecessor to AGENTS.md, being folded in - check both until merged", "owner": "docs" },
  "index.html": { "purpose": "Legacy content+engine source of truth (41K+ LOC) for tracks not yet migrated to content/", "owner": "content-legacy" },
  "build.js": { "purpose": "Extracts CSS/JS/per-track HTML from index.html; defines the baseNjk page-shell template string", "owner": "build" },
  "course_schema.json": { "purpose": "JSON Schema for content/{track}/*.json chapter files (target content system). Bilingual {en,ar} fields, videos[], explorers[] added WP5. See docs/CONTENT_MODEL.md", "owner": "content" },
  "app.py": { "purpose": "Flask reverse-proxy for the AI chat feature - separate deployable, NOT built by GitHub Pages CI", "owner": "backend" },
  "parse_notes.py": { "purpose": "DOCX -> course_schema.json migration tool", "owner": "tooling" },
  "sw.js": { "purpose": "Service worker; precache list + SW_VERSION cache-bust string", "owner": "build" },
  "manifest.json": { "purpose": "PWA manifest", "owner": "config" },
  "sitemap.xml": { "purpose": "Hand-maintained sitemap, one <url> per track + home", "owner": "config" },
  "robots.txt": { "purpose": "Crawler config, points to sitemap.xml", "owner": "config" },
  ".eleventy.js": { "purpose": "Eleventy config: input/output dirs, passthrough-copy list (must list every standalone root HTML page by hand)", "owner": "build" },
  "package.json": { "purpose": "npm scripts (build, start) and the 3 devDependencies", "owner": "config" },
  ".github/workflows/deploy.yml": { "purpose": "CI: npm run build -> deploy _site/ to gh-pages on push to main", "owner": "build" },

  "changelog.html": { "purpose": "Standalone page, own header/dark-mode toggle, not an Eleventy template", "owner": "content" },
  "rigor-standard.html": { "purpose": "Standalone page, draft pending sign-off", "owner": "content" },
  "free-tier-promise.html": { "purpose": "Standalone page, draft pending sign-off; describes pricing that is copy-only, not implemented", "owner": "content" },

  "src/_includes/base.njk": { "purpose": "Generated from build.js's baseNjk string for legacy tracks - do not hand-edit until WP3 (see docs/PHASE1-2_TARGET_ARCHITECTURE.md) retires this pattern", "owner": "build" },
  "src/_includes/tracks/": { "purpose": "Per-track HTML, generated by build.js from index.html - do not hand-edit, 21 files", "owner": "build" },
  "src/_includes/legal/": { "purpose": "Hand-authored small legal-page content includes", "owner": "content" },
  "src/_includes/partials/": { "purpose": "NEW (target system): reusable chapter/quiz/explorer/video Nunjucks components, see docs/CONTENT_MODEL.md", "owner": "content" },
  "src/{track}/index.njk": { "purpose": "21 thin (8-LOC) wrappers, one per track, set trackId and include the matching content", "owner": "content" },

  "content/": { "purpose": "NEW (target system): content/{track}/{chapter-slug}.json, schema-validated against course_schema.json. Pilot track: qudrat", "owner": "content" },
  "bank-data/": { "purpose": "Per-track MCQ question pools (21 files), fetched client-side at runtime - already the target pattern, do not restructure", "owner": "content" },
  "courses/": { "purpose": "Currently unused (.gitkeep only) - distinct from content/, do not assume interchangeable", "owner": "unknown" },

  "public/css/main.css": { "purpose": "Design tokens (87 CSS custom properties) + component styles, extracted by build.js for legacy tracks. See docs/DESIGN_TOKENS.md", "owner": "style" },
  "public/js/engine.js": { "purpose": "Extracted inline JS from index.html - quiz generation, explorer canvases, UI logic", "owner": "engine" },
  "public/js/cloud-sync.js": { "purpose": "Supabase-backed localStorage mirror for cross-device progress sync", "owner": "engine" },
  "public/js/cloud-config.js": { "purpose": "Supabase project keys for cloud-sync.js", "owner": "config" },
  "public/js/teacher-view.js": { "purpose": "Teacher/parent class-code view feature", "owner": "engine" },
  "public/downloads/": { "purpose": "1,880 worksheet/answer-key PDFs, one pair per topic per track - content asset, exclude from AI context reads", "owner": "content-asset" },


  "supabase/schema.sql": { "purpose": "DB schema + RLS policies for the cloud-sync feature", "owner": "backend" },
  "tools/worksheet_gen/": { "purpose": "Standalone PDF worksheet generator, not wired into the build/CI graph", "owner": "tooling" },

  "docs/INVENTORY.md": { "purpose": "Phase 0 baseline audit (point-in-time, historical record)", "owner": "docs" },
  "docs/PHASE1-2_TARGET_ARCHITECTURE.md": { "purpose": "Active migration plan - principles, target architecture, work-package sequence", "owner": "docs" },
  "docs/RECIPES.md": { "purpose": "Exact file-level steps for the 10 benchmark maintenance tasks", "owner": "docs" },
  "docs/CONTENT_MODEL.md": { "purpose": "content/{track}/*.json shape, the bilingual field rule, worked example", "owner": "docs" },
  "docs/DESIGN_TOKENS.md": { "purpose": "Token names/values/usage rules for public/css/main.css", "owner": "docs" },
  "docs/MAP.json": { "purpose": "This file", "owner": "docs" },
  "docs/DECISIONS/": { "purpose": "One short ADR per significant architecture decision", "owner": "docs" },
  "scripts/sweep-hex-to-tokens.py": { "purpose": "Finds/replaces hardcoded hex colors in index.html's <style> blocks that exactly duplicate a design token (WP2)", "owner": "tooling" },
  "scripts/check-shell-sync.js": { "purpose": "Build-time check: fails if index.html's header/footer drifts from build.js's baseNjk header/footer (WP3). Run via npm run build or npm run check-shell-sync", "owner": "tooling" },
  "scripts/validate-content.js": { "purpose": "Build-time check: validates content/{track}/*.json against course_schema.json via ajv (WP5). Run via npm run build or npm run validate-content", "owner": "tooling" },

  "AUDIT.md": { "purpose": "Pre-Eleventy-migration Phase 0 audit (2026-07-02) - historical record, largely superseded by docs/INVENTORY.md", "owner": "docs" },
  "DEDUP_LOG.md": { "purpose": "Log of duplicate content blocks removed during migration - currently empty (0 removed)", "owner": "docs" },
  "MATH_ERRATA.md": { "purpose": "Log of mathematical errors found during review - currently empty (0 found)", "owner": "docs" },
  "SUPABASE_SETUP.md": { "purpose": "Setup guide for the optional cloud-sync/accounts feature", "owner": "docs" }
}
