# ── Backup / working copies (DO NOT publish) ──
index_*.html
mathema-hub*.html

# ── OS junk ──
.DS_Store
Thumbs.db
desktop.ini

# ── Editor / IDE ──
.vscode/
.idea/
*.swp
*.swo
*~

# ── Secrets / keys ──
.env
.env.*
secrets.js
config.local.*

# ── Node (if ever used) ──
node_modules/
