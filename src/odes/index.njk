---
layout: base.njk
trackId: odes
title: "Differential Equations · ClipSAT"
description: "First-order and separable differential equations — proof-first, with full derivations, matching the site's Rigor Standard."
isRtl: false
---
{#
  Renders from content/odes/* (docs/CONTENT_MODEL.md), following the exact pattern established by
  linalg/index.njk and mvc/index.njk — a brand-new track (not a legacy cutover), Content Excellence
  roadmap Pillar 1's "launch Multivariable Calc/Linear Algebra/ODEs" item (odes is the 3rd and
  final track named there — mvc and linalg already shipped).

  Scoped as a deliberately small first slice, narrower than mvc/linalg's initial 4-chapter launches:
  ONE chapter (First-Order Differential Equations & Separable Equations), with a real "thm"+"pf"
  pair (Theorem 1.1, proved via the chain rule — not just "separate and integrate" asserted), two
  worked examples plus a real-world application (radioactive decay / half-life), matching Pillar 1's
  rubric. No explorer block yet — same reasoning as linalg/mvc's own initial launches: a canvas
  explorer (a slope-field / solution-curve visualizer would be the natural fit here) is separate
  engine.js work, tracked as follow-up, not silently implied as done.

  bank-data/odes.json ships as an honest starter bank (9 questions, 3 easy/3 medium/3 hard, one
  domain: "Separable Equations") — same starter density as linalg/mvc's original launches, below
  the site's 50/50/50-per-domain standard every longer-established track's bank meets. Scaling it
  up (more chapters, more bank domains) is separate, tracked follow-up work.
#}
{% set odes = migratedContent.odes %}
{% set pageHeader = odes.meta.meta.pageHeader %}
<main id="view-odes" class="view active">
  {% include "partials/subject-head.njk" %}

  <div class="wrap calc-body">
    {% set chapters = odes.chapters %}
    {% set practiceSetId = 'odes-practice' %}
    {% set testGeneratorId = 'odes-testgen' %}
    {% set downloadsId = 'odes-downloads' %}
    {% include "partials/rail.njk" %}

    <div class="content">
      {% for chapter in odes.chapters %}
        {% include "partials/chapter.njk" %}
      {% endfor %}

      {% set sectionId = 'odes-practice' %}
      {% set practiceSet = odes.practiceSet %}
      {% include "partials/practice-set.njk" %}

      {% set sectionId = 'odes-testgen' %}
      {% set testGenerator = odes.meta.meta.testGenerator %}
      {% include "partials/test-generator.njk" %}

      {% set sectionId = 'odes-downloads' %}
      {% set courseMeta = odes.meta.meta %}
      {% include "partials/downloads-block.njk" %}
    </div>
  </div>
</main>
